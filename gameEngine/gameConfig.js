const numRows = 10;
const numColumns = 10;
const gameWidth = 800;
const gameHeight = 800;

module.exports = Object.freeze({
    numRows: numRows,
    numColumns: numColumns,
    gameWidth: gameWidth,
    gameHeight: gameHeight,
});